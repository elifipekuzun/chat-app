import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  marginVertical6: {
    marginVertical: 6,
  },
  alertContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'flex-start',
  },
});
